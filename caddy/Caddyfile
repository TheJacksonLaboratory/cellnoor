# TODO: h2c instead of http1 because http2 is faster
{$PUBLIC_URL} {
    {$TLS_SETTINGS}
    reverse_proxy /api/auth/* {$UI_HOST}:{$UI_PORT}
    reverse_proxy /api/* {$API_HOST}:{$API_PORT}
    reverse_proxy /* {$UI_HOST}:{$UI_PORT}
}
