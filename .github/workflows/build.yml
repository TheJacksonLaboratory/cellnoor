name: Build
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build_cellnoor-api
        run: |
          VERSION=$(cargo metadata | jq --raw-output '.packages[] | select(.name == "cellnoor-api").version')
