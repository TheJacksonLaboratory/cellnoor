[package]
edition.workspace = true
name = "default-vec"
version = "0.1.0"

[dependencies]
diesel = { workspace = true, optional = true }
serde = { workspace = true, optional = true }

[lints]
workspace = true

[features]
diesel = ["dep:diesel"]
serde = ["dep:serde"]
